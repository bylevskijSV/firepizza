<% provide :page_title, 'Пользователи' %>
<% content_for :head do %>
  <%= stylesheet_link_tag 'styles_delivery', media: 'all', 'data-turbolinks-track': 'reload' %>
<% end %>

<%= pagination @pagy %>

<section>
  <div class="cart">
    <div class="cart-headline">
      <p>Admin panel</p>
    </div>
    <div class="cart-content">
      <%= link_to 'Cкачать zip архив xlsx', admin_users_path(format: :zip) %>
      <table>
        <thead>
          <tr>
            <th>#</th>
            <th>Имя</th>
            <th>Email</th>
            <th>Created at</th>
            <th>Updated at</th>
          </tr>
        </thead>
        <tbody>
          <%= render @users %>
        </tbody>
      </table>
      <div class="delivery-content-form-minsk">
        Загрузить пользователей из XLSX

        <%= form_with url: admin_users_path do |f|%>
          <%= f.file_field :archive %>
          <%= f.submit 'Загрузить', class: "send-email" %>
        <% end %>
      </div>
    </div>
    
  </div>
</section>

<%= pagination @pagy%>
